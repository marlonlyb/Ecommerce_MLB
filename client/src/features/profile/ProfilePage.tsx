import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export function ProfilePage() {
  return (
    <PagePlaceholder
      title="Profile placeholder"
      description="The authenticated account area is scaffolded and ready for Phase 4."
    />
  );
}
