import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export function OrdersPage() {
  return (
    <PagePlaceholder
      title="Orders placeholder"
      description="Order history UI will connect once session and orders API are implemented."
    />
  );
}
