import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export function CatalogPage() {
  return (
    <PagePlaceholder
      title="Catalog foundation ready"
      description="Phase 1 leaves the public storefront shell ready for product listing in Phase 3."
    />
  );
}
