import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export function CheckoutPage() {
  return (
    <PagePlaceholder
      title="Checkout shell"
      description="The protected checkout route is in place; PayPal flow arrives in Phase 5."
    />
  );
}
